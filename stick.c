#include<stdio.h>

int main(){
    int x;
    int n = 64;
    int sum = 0;

    scanf("%d", &x);

    for(i=0;;i++){
        n / 2
        if(x>=n) sum += n;
        printf("%d", sum);
    }
    return 0;
}